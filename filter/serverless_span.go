package filter

// TODO: Use filter & data generate as interface

// Encoding part parsing necessary information from the span and return them according to the error type
// and encode them into the request header.

type ServerlessSpanData struct {
}
